apiVersion: v1
kind: Service
metadata:
  name: django-app
  labels:
    app: django-app
spec:
  type: {{ .Values.service.type }}
  selector:
    app: django-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
